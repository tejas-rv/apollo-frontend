import React,{useState} from "react"; import {api} from "../services/api"; export default function Settings(){const [msg,setMsg]=useState(""),[err,setErr]=useState("");async function refresh(){setMsg("");setErr("");try{await api.refreshSecurity();setMsg("Security configuration refreshed successfully.")}catch(e){setErr(e.message)}}return <section className="page narrow"><div className="page-head"><div><span className="eyebrow">ADMIN</span><h1>Security</h1><p className="muted">Runtime security configuration controls.</p></div></div><div className="panel"><h2>JWT / security configuration</h2><p className="muted">Use this after rotating the backend system secret so the application reloads its security configuration without a restart.</p>{msg&&<div className="alert success">{msg}</div>}{err&&<div className="alert error">{err}</div>}<button className="primary" onClick={refresh}>Refresh configuration</button></div></section>}