import React,{useState} from "react"; import {Mail,MessageCircle} from "lucide-react"; import {api} from "../services/api";
export default function Notifications(){const [tab,setTab]=useState("email"),[mode,setMode]=useState("plain"),[form,setForm]=useState({}),[message,setMessage]=useState(""),[error,setError]=useState(""),[loading,setLoading]=useState(false);const set=e=>setForm({...form,[e.target.name]:e.target.value});
async function send(e){e.preventDefault();setLoading(true);setMessage("");setError("");try{const fn=tab==="email"?(mode==="contract"?api.contractEmail:api.email):(mode==="contract"?api.contractWhatsapp:api.whatsapp);const r=await fn(form);setMessage(`Notification sent successfully${r?.id?` (#${r.id})`:""}`);setForm({})}catch(x){setError(x.message)}finally{setLoading(false)}}
return <section className="page narrow"><div className="page-head"><div><span className="eyebrow">COMMUNICATIONS</span><h1>Notifications</h1><p className="muted">Send email or WhatsApp messages from the admin account.</p></div></div><div className="tabs"><button className={tab==="email"?"selected":""} onClick={()=>setTab("email")}><Mail size={17}/> Email</button><button className={tab==="whatsapp"?"selected":""} onClick={()=>setTab("whatsapp")}><MessageCircle size={17}/> WhatsApp</button></div><div className="segmented"><button className={mode==="plain"?"selected":""} onClick={()=>setMode("plain")}>Plain message</button><button className={mode==="contract"?"selected":""} onClick={()=>setMode("contract")}>AMC contract</button></div>{message&&<div className="alert success">{message}</div>}{error&&<div className="alert error">{error}</div>}<form className="panel form" onSubmit={send}>{mode==="contract"&&<Field label="Customer ID *" name="customerId" value={form.customerId} onChange={set} type="number"/>}{tab==="email"?<Field label="Recipient email *" name="email" value={form.email} onChange={set} type="email"/>:<Field label="Phone number *" name="phoneNumber" value={form.phoneNumber} onChange={set} placeholder="+919876543210"/>}{mode==="plain"?tab==="email"?<><Field label="Subject *" name="subject" value={form.subject} onChange={set}/><label>Body *<textarea name="body" rows="8" value={form.body||""} onChange={set}/></label></>:<label>Message *<textarea name="message" rows="8" value={form.message||""} onChange={set}/></label>:<><Field label={tab==="email"?"Subject":"Caption"} name={tab==="email"?"subject":"caption"} value={form[tab==="email"?"subject":"caption"]} onChange={set}/><label>{tab==="email"?"Body":"Reference key"}{tab==="email"?<textarea name="body" rows="6" value={form.body||""} onChange={set}/>:<input name="referenceKey" value={form.referenceKey||""} onChange={set}/>}</label></>}<button className="primary wide" disabled={loading}>{loading?"Sending…":`Send ${tab}`}</button></form></section>}
function Field({label,name,value,onChange,type="text",placeholder}){return <label>{label}<input name={name} type={type} value={value||""} onChange={onChange} placeholder={placeholder}/></label>}